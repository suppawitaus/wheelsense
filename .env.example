# MQTT (internal to compose network)
MQTT_URL=mqtt://mosquitto:1883
MQTT_USER=dev
MQTT_PASS=devpass

# API service (FastAPI) — add more as we build Step 3
API_PORT=8000

# Web (Next.js) — used by frontend to call API
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Home Assistant (not used yet; placeholder for Step 5)
HA_BASE_URL=http://homeassistant.local:8123
HA_TOKEN=

# Timezone/display policy
APP_TIMEZONE=Asia/Bangkok
